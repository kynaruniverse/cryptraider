{
  "name": "crypt-raider-reborn",
  "version": "1.0.0",
  "description": "Modern Crypt Raider rebuild",
  "scripts": {
    "build": "echo 'No build step required'",
    "sync": "npx cap sync",
    "web": "echo 'static web project'"
  },
  "dependencies": {
    "@capacitor/core": "^5.0.0"
  }
}