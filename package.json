{
  "name": "crypt-raider",
  "version": "1.0.0",
  "description": "Crypt Raider — Full recreation of the 2003 Flash classic",
  "type": "module",
  "scripts": {
    "dev":     "vite",
    "build":   "vite build",
    "preview": "vite preview",
    "copy":    "cap copy",
    "sync":    "cap sync",
    "android": "npm run build && cap sync android && cap open android",
    "ios":     "npm run build && cap sync ios && cap open ios"
  },
  "dependencies": {
    "@capacitor/core":    "^5.7.0",
    "@capacitor/android": "^5.7.0",
    "@capacitor/ios":     "^5.7.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.7.0",
    "vite":           "^5.2.0"
  },
  "license": "MIT"
}
