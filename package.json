{
  "name": "crypt-raider",
  "version": "1.0.0",
  "description": "Crypt Raider — Full recreation of the 2003 Flash classic",
  "main": "index.html",
  "scripts": {
    "start": "serve . -l 3000",
    "build": "echo 'Pure ESM — ready to sync'",
    "copy": "cap copy",
    "sync": "cap sync",
    "android": "cap sync android && cap open android",
    "ios": "cap sync ios && cap open ios"
  },

  "dependencies": {
    "@capacitor/core": "^5.7.0",
    "@capacitor/android": "^5.7.0",
    "@capacitor/ios": "^5.7.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.7.0",
    "serve": "^14.2.0"
  },

  "license": "MIT"
}
